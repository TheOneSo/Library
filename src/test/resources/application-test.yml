spring:
  shell:
    interactive:
      enabled: false

  datasource:
    url: jdbc:h2:mem:db1;MODE=PostgreSQL

  liquibase:
    enabled: true
    drop-first: true
    change-log: classpath:/db.changelog/db.changelog-master-test.yaml

  jpa:
    hibernate:
      ddl-auto: none