spring:
  shell:
    interactive:
      enabled: false

  datasource:
    initialization-mode: always
    url: jdbc:h2:mem:db1;MODE=PostgreSQL
    schema: schema.sql
    data: testdata.sql

  liquibase:
    enabled: false

  jpa:
    hibernate:
      ddl-auto: none